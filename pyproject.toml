[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crop-disease-detection"
version = "0.1.0"
description = "Simple scaffold for crop disease detection using TensorFlow/Keras"
authors = [ { name = "Pratiksha" } ]
